.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
.coverage

# Debian package build artifacts (built from debian/ on demand, not checked in)
.pybuild/
debian/.debhelper/
debian/july-ibus/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.debhelper
debian/*.debhelper.log
build/

# Claude Code harness-managed files, not project source
.agents/
.claude/
skills-lock.json

# Working/planning material for building this project with an AI coding
# agent — not part of the shipped product, kept out of the repo.
Bornona.txt
SPEC.md
tasks/

# Built release packages (.deb, Arch .pkg.tar.zst) -- built on demand,
# not checked in. Kept here (rather than the parent directory) so
# they're easy to find alongside the repo.
dist-packages/
